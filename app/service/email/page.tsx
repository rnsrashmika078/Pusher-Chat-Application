import EmailLayout from "@/src/compnents/Email/EmailLayout";
import React from "react";

const page = () => {
  return (
    <div>
      <EmailLayout emails={[]} />
    </div>
  );
};

export default page;
